# esbuild-plugin-pino declares a narrower peer range than the supported
# esbuild version used by this project. npm must keep the existing workspace
# dependency graph instead of failing on that peer declaration.
legacy-peer-deps=true
